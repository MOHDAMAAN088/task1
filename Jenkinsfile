pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo '🔧 Building the application...'
                // For example: sh 'python setup.py build'
            }
        }

        stage('Test') {
            steps {
                echo '🧪 Running tests...'
                // For example: sh 'pytest'
            }
        }

        stage('Deploy') {
            steps {
                echo '🚀 Deploying the application...'
                // You can simulate deploy using echo or use Docker
            }
        }
    }
}
